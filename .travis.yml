language: cpp
matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
    - os: osx

before_install:
  - ./travis/install-deps.sh

script:
  - qmake -r
  - make
  - cd ./test
  - ./test -platform offscreen

after_success:
  - cd ..
  - ./travis/publish-docs.sh
